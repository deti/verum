# [Project Name] — Architecture Decisions

> [System-level decisions that affect multiple components or the project as a whole]

<!-- This file records architectural decisions that are too broad to belong in any
     single component's Decisions section. Technology choices, communication
     patterns, deployment topology, data flow direction — anything that shapes
     how components relate to each other.

     Each entry uses a bold date prefix followed by the decision and its
     reasoning. Write the reasoning, not just the conclusion — a future reader
     needs to understand why, not just what.

     Decisions scoped to one component belong in that component's Decisions
     section, not here. If a decision started component-scoped but grew to
     affect the system, move it here and leave a cross-reference. -->

- **[YYYY-MM-DD]: [Decision title].** [Reasoning in 1-2 sentences explaining why this choice was made and what alternatives were considered or rejected.]
