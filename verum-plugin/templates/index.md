# [Project Name]

> [One-line description of what this system does]

<!-- The project index is the entry point for your spec. It lists every component,
     states invariants that hold across the entire system, and records architecture
     decisions that affect more than one component. Keep it short — details belong
     in the component files. -->

## Components

<!-- List every component as a link to its spec file. One line per component.
     Format: - [Name](components/component-name.md) — what it does
     Order doesn't imply priority. Alphabetical or dependency order both work. -->

- [Component Name](components/component-name.md) — [what it does]

## System Invariants

<!-- Invariants here apply across ALL components. If an invariant only applies to
     one component, put it in that component's Invariants section instead.
     These are properties the system must maintain at all times, not goals or
     aspirations. Write them as falsifiable statements. -->

- [Invariant that holds across all components]

## Architecture Decisions

<!-- Record decisions that affect the system as a whole — technology choices,
     communication patterns, data flow direction. Decisions scoped to a single
     component belong in that component's Decisions section.
     Use bold date prefix. Write the reasoning, not just the conclusion. -->

- **[YYYY-MM-DD]: [Decision title].** [Reasoning in 1-2 sentences.]
